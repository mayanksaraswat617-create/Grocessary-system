# Render Blueprint for Dockerized PHP Deployment
services:
  - type: web
    name: groceesary-market
    runtime: docker # Force Docker mode (Standard PHP with SQLite/Apache)
    plan: free
    region: ohio
    envVars:
      - key: RENDER
        value: true
